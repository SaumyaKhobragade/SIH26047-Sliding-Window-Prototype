# MediKiosk Prototype Backend — Dependencies

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
python-multipart==0.0.9
pydantic==2.9.0

# Face Recognition (dlib-based, 128-d embeddings)
# NOTE: On Windows, you may need to install cmake first:
#   pip install cmake
#   pip install dlib
#   pip install face-recognition
face-recognition==1.3.0

# Drug name fuzzy matching
rapidfuzz==3.9.0

# Sarvam AI SDK (STT/TTS/Vision)
sarvamai>=0.1.31

# LLM Providers (optional — for production extraction)
# groq==0.11.0
# google-generativeai==0.8.0

# NumPy (required by face_recognition)
numpy>=1.24.0

# HTTP client
httpx==0.27.0
